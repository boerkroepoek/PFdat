# Peilfilterlogger naar Excel

## Starten

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements-dev.txt
streamlit run app.py
```

Windows PowerShell gebruikt voor activatie: `.venv\Scripts\Activate.ps1`.

## Testen

```bash
pytest -q
```
